{
  "name": "apisql",
  "version": "1.0.0",
  "description": "l'api sql du projet stock collocation",
  "license": "MIT",
  "author": "groupe stock collocation",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node --env-file=.env dist/index.js ",
    "dev": "tsc --watch & node --watch dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "install": "^0.13.0",
    "npm": "^11.2.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mariadb": "^3.4.0",
    "mqtt": "^5.10.4"
  }
}
